<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Create a Food</title>
  </head>
  <body>
    <h1>Edit <%= food.name %></h1>
    <form action="/food/<%= food._id %>?_method=PUT" method="POST">
      <label for="name">Name:</label>
      <input type="text" name="name" id="name" value="<%= food.name %>"/>

      <div class="checkbox-div">
        <label for="ready-to-eat">Ready to Eat?</label>
        <input type="checkbox" name="isReadyToEat" id="ready-to-eat" <% if (food.isReadyToEat) { %> checked <% } %> />
      </div>
        
      <button type="submit">Update Food</button>
    </form>

    <a class="food-index-link" href="/food">
      <div class="index-link-text">Back to Food</div>
    </a>
  </body>
</html>